name = "eksisozluk-gundem-worker"
main = "cloudflare-worker.js"
compatibility_date = "2024-01-01"

# Cloudflare Workers AI binding
# Note: You need to enable Workers AI in your Cloudflare dashboard
# and bind it to your worker. In production, add this binding in the dashboard.
# For local development, use: wrangler dev --remote

[env.production]
name = "eksisozluk-gundem-worker"

[env.development]
name = "eksisozluk-gundem-worker-dev"

